/**
 * Static site identity (authored content, not captured data) — U10.
 *
 * The showcase's numbers come from the JSON store via the view-model; this is the
 * authored copy that frames them (Josh's voice). One place to edit identity.
 */
export const PROFILE = {
  name: 'Josh Gonzales',
  title: 'PhD Candidate',
  /** Hero statement, split so the page can emphasize the tail. */
  statementLead: 'I build and ship',
  statementEm: 'research tools',
  githubUrl: 'https://github.com/phdemotions',
} as const
